
Процедура ЗаписатьДвичныеДанные(СсылкаНаобъект, ИмяФайла) Экспорт
	
	Если ТипЗнч(СсылкаНаобъект.Хранилище) <> Тип("ДвоичныеДанные") Тогда
		ДвоичныеДанные = СсылкаНаобъект.Хранилище.Получить();
	Иначе
		ДвоичныеДанные = СсылкаНаобъект.Хранилище;
	КонецЕсли; 
	
	ДвоичныеДанные.Записать(ИмяФайла);

КонецПроцедуры // ЗаписатьДвичныеДанные()
                                               